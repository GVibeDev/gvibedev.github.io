# GV-PORTFOLIO V0.10 — Arena Rubra Card Composer Showcase

## Validation checklist

- [ ] `/web-apps/` lists Arena Rubra Card Composer with its new card image.
- [ ] `/projects/arena-rubra-card-composer/` loads without content/schema errors.
- [ ] Hero image renders at full width without distortion.
- [ ] Status reads `Step 2A — Active Development`.
- [ ] Repository CTA points to `https://github.com/GVibeDev/Card-Composer-Arena-Rubra`.
- [ ] No `Launch App` CTA is shown yet.
- [ ] Nexus, Exordium, Agathoi and Liberti gallery images open correctly.
- [ ] Mobile layout remains readable and gallery dialog can be closed.
- [ ] `/projects/arena-rubra-board-game/` still works.
- [ ] `/projects/bw-survivors/` and `/apps/bw-survivors/` still work.
- [ ] `npm run build` succeeds.
- [ ] `npm run preview` serves the project page correctly.

## Scope guard

V0.10 only promotes the existing Card Composer placeholder into a verified showcase page using the supplied Step 2A captures and repository URL. It does not modify the Card Composer repository and does not create or claim a hosted runtime.
